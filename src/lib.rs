pub mod cpu;

pub const WIDTH: usize = 64;
pub const HEIGHT: usize = 32;
pub const SCALE: u32 = 20;
pub const SCREEN_WIDTH: u32 = (WIDTH as u32) * SCALE;
pub const SCREEN_HEIGHT: u32 = (HEIGHT as u32) * SCALE;
